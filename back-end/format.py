# import json

# def format_adoption_center_in_schema(schema):
# 	if "center" in schema:
# 		schema["center"] = {
# 			"name": schema["center"]["name"],
# 			"number": schema["center"]['api_id']
# 		}

